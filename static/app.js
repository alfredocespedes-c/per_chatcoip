const API_BASE=(window.CHATCOIP_API_BASE||"").replace(/\/$/,"");const form=document.querySelector('#form');const input=document.querySelector('#question');const messages=document.querySelector('#messages');const send=document.querySelector('#send');document.querySelectorAll('.examples button').forEach(b=>b.addEventListener('click',()=>{input.value=b.textContent;input.focus()}));function add(text,type){const row=document.createElement('div');row.className=`message ${type}`;row.innerHTML=type==='bot'?`<div class="avatar">C</div><div class="bubble"><b>ChatCoip</b><p></p></div>`:`<div class="bubble"><p></p></div>`;row.querySelector('p').textContent=text;messages.appendChild(row);messages.scrollTop=messages.scrollHeight;return row}form.addEventListener('submit',async e=>{e.preventDefault();const q=input.value.trim();if(!q)return;add(q,'user');input.value='';send.disabled=true;send.innerHTML='Consultando… 🦫';const pending=add('Estoy consultando los datos…','bot');try{const r=await fetch(`${API_BASE}/api/preguntar`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({pregunta:q})});const data=await r.json();if(!r.ok)throw new Error(data.detail||'No fue posible realizar la consulta.');pending.querySelector('p').textContent=data.respuesta}catch(err){pending.querySelector('p').textContent=`No pude completar la consulta: ${err.message}`}finally{send.disabled=false;send.innerHTML='Preguntar <span>🦫</span>';input.focus()}});input.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();form.requestSubmit()}});